20140505
エクセルのproxy的なの（？）を作成した。
基本的な実装のみの作成。
（それ以上を使うとは思えない。）

動かし方は"test_excel.py"におおまかに記述しているので、そちらを参考に。

あと必要そうな機能がでたら追加する。
極力1モジュールないで完結する。
多くなった場合、中継のクラスを作成してそれぞれの動作を分離してしまう。

現時点で考えられる必要そうな機能
・フォントの大きさの指定
・フォントの設定
・セルの大きさの設定
・マクロの設定（Python側ですれば良いような。。）
・色の設定
基本的には見ないと分からないので、なくても困らないかと。
